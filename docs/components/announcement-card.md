# Announcement Card

Displays announcement title, message preview, publication time, and optional event context/link.

**Action:** opens the announcement detail or approved destination.

**Security:** component receives only announcements already filtered by authorized query/rules.

**States:** loading, normal, long message, empty optional fields.